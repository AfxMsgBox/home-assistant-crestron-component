# 本地运行测试所需依赖。
#
# 测试只覆盖不依赖 Home Assistant 运行时的纯逻辑模块（见 CLAUDE.md），
# 因此依赖很少：只有 tests/test_schema.py 需要 voluptuous，其余为纯标准库。
#
#   pip install -r requirements-test.txt
#   python3 -m unittest discover -s tests
#   mypy --config-file mypy.ini    # type-check the pure-logic modules
voluptuous
mypy
