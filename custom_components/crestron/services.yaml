reload:
  name: Reload
  description: >-
    Re-read the `crestron:` block from configuration.yaml and reload the
    integration. Use after editing (or regenerating) crestron.yaml instead of
    restarting Home Assistant.
